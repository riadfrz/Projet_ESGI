{
  "name": "projet-esgi",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "format:all": "cd frontend && pnpm format && cd ../backend && pnpm format && cd ../shared && pnpm format"
  }
}